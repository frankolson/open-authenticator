{
  "name": "open-authenticator",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "ssc build -r -o --test=./test/index.js --headless",
    "init-project": "ssc init --config",
    "start": "ssc build -r -o",
    "dev": "ssc build -r -o --watch",
    "install:ios": "ssc build --platform=ios -c -p && ssc install-app --platform=ios",
    "install:android": "ssc build --platform=android -c -p && ssc install-app --platform=android",
    "build": "ssc build -o"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "esbuild": "^0.23.0"
  },
  "dependencies": {
    "@phosphor-icons/react": "^2.1.7",
    "@types/node": "^22.0.2",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "jsqr": "^1.4.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "totp-generator": "^1.0.0",
    "typescript": "^5.5.4"
  },
  "type": "module"
}
